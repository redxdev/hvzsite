<div class="container">
    <div class="vertical-space"></div>

    <div class="row">
        <div class="col-sm-12 text-center">
            <div class="col-sm-12 text-center">
                <h2>Change Avatar</h2>
                <h3>{{model.player.name}} : {{model.player.email}}</h3>
            </div>
        </div>
        <div class="col-sm-4 col-sm-offset-4">
            <div id="webcam"></div>
            <button id="pictureButton" class="btn btn-primary" {{action 'takePicture' model.player.id}}>Take Picture</button>
        </div>
    </div>
</div>